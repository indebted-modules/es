version: "3.1"
services:
  test:
    build:
      context: .
      dockerfile: ./images/test.Dockerfile
    volumes:
      - ../:/src
    working_dir: /src
    depends_on:
      - dynamodb

  dynamodb:
    image: amazon/dynamodb-local:1.11.477
    ports:
      - "8000:8000"
